#Listar todos los usuarios cuyo nombre comience con la letra “a”
SELECT nombre  FROM usuario WHERE nombre like 'a%';
SELECT * FROM avatar WHERE nombre like 'carita feliz';
#Listar todos los usuarios que no hayan cargado el email.
SELECT nombre,email FROM usuario WHERE email IS NULL;
#Mostrar todos los canales creados entre el 01/04/2021 y el 01/06/2021
SELECT * FROM canal WHERE fechaCreacion BETWEEN '2021-04-01' AND '2021-06-01';
#Listar los 10 usuarios más jóvenes.
SELECT nombre, fechaNacimiento  FROM usuario ORDER BY fechaNacimiento DESC  LIMIT 10;
#Mostrar todas las playlists que sean privadas
SELECT nombre, privado FROM playlist WHERE privado = 1; 
#Listar el top 5 de videos con más cantidad de likes.
SELECT titulo, cantidadLikes FROM video ORDER BY cantidadLikes DESC LIMIT 5;
#Insertar un usuario con los siguientes datos
INSERT INTO usuario (
idUsuario,
nombre,
email,
password,
fechaNacimiento,
codigoPostal,
Pais_idPais,
Avatar_idAvatar
)
VALUES( 20, "Juan Jose Batzal","jjbatzal@gmail.com", "jj1597", "2000-04-01", "1429", 9, 85
);

#Generar un listado con todos los usuarios que hayan nacido en el año 2000
SELECT nombre, fechaNacimiento FROM usuario WHERE fechaNacimiento LIKE '2000%';
#Listar todos los países ordenados alfabéticamente y su nombre en mayúsculas
#Listar todos los videos que posean más de 500.000 reproducciones.
SELECT titulo, cantidadReproducciones FROM video WHERE cantidadReproducciones > 500000;
#Generar un reporte de todos los videos publicados en el año 2020 que sean privados y que posean más de 100 dislikes.
SELECT titulo, fechaPublicacion, privado, cantidadDislikes FROM video WHERE fechaPublicacion LIKE '2020%' AND cantidadDislikes > 100 AND privado = 1;
SELECT * FROM video where fechaPublicacion like '%2020%' and  privado = 1 and cantidadDislikes > 100;
#Por error hemos cargado mal los datos de Juan Jose Batzal. La fecha de
#nacimiento no era 01/04/2000, sino 04/01/2000. Ahora debemos crear y
#ejecutar la sentencia necesaria para realizar la modificación.
UPDATE usuario SET fechaNacimiento = '2000/01/04'WHERE `idUsuario` = 20;
#Listar todos los usuarios cuyo password contenga menos de 5 caracteres
#(pista: ver la función length()).
SELECT * FROM usuario WHERE length(password)  < 5;
#Generar un reporte de los usuarios. En la consulta mostrar los siguientes
-- títulos:
-- ID Nombre E-mail Fecha de
-- Nacimiento
-- Edad Codigo de
-- Pais
SELECT idUsuario AS ID, nombre AS Nombre, email AS "E-mail",
fechanacimiento AS "Fecha de Nacimiento", pais_idpais AS "Codigo de Pais" FROM usuario;
#Listar el video de mayor tamaño.
SELECT idVideo, titulo, max(tamanio) FROM video;
#Generar un reporte de las últimas 10 reacciones, listando la fecha, el código
#de reacción, el ID de usuario y el video.
SELECT fecha, idreaccion, usuario_idusuario, video_idvideo from reaccion order by fecha desc limit 10;
#Listar todos los videos que tengan menos de 100.000 reproducciones y 100 o
-- más likes.
SELECT * FROM video WHERE cantidadreproducciones < 100000 AND cantidadlikes >= 100;
#Mostrar todos los videos que incluyan la palabra “FAN” dentro de su descripcion
SELECT * FROM video WHERE descripcion LIKE "%FAN%";
#Generar un listado de los usuarios cuyos passwords sean aquellos que no
-- satisfacen las políticas de seguridad de la empresa. Estos son los que se
-- corresponden con: 123, 1234, 12345, abc, clave, password.
SELECT * FROM usuario WHERE password IN ("123","1234","12345","clave","password","abc");
#Eliminar el avatar cuyo nombre sea avDhTube.
DELETE FROM avatar WHERE idAvatar = 84;